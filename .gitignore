# Real LAN layout / per-machine config. Keep it off GitHub
printers.yaml
users.yaml

# Secrets (auth credentials). Never commit these
.env
printdeck.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Docker compose default data directory (printers.yaml + users.yaml live here)
data/

# Tooling caches
.ruff_cache/
