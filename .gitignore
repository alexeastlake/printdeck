# Real LAN layout / per-machine config — keep it off GitHub
printers.yaml

# Secrets (auth credentials) — never commit these
.env
printdeck.env

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
