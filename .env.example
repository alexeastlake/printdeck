# Copy to .env (gitignored) and fill in. docker compose reads this automatically.
# Leave blank to run without a login.
PRINTDECK_USERNAME=you
PRINTDECK_PASSWORD=change-me
# Signs the session cookie; keep it long and random. Without it, logins reset
# whenever the container restarts.
PRINTDECK_SECRET=generate-a-long-random-string
